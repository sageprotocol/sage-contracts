[package]
name = "SagePost"
edition = "2024.beta"
version = "0.0.1"

[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "framework/mainnet" }

SageAdmin = { local = "../admin" }
SageChannel = { local = "../channel" }
SageTable = { local = "../table" }

[addresses]
sage = "0x0"
